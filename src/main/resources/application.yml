
server:
  port: 8085

spring:
    cloud.compatibility-verifier.enabled: false
    datasource:
      url: jdbc:h2:mem:productOrder_data
      driverClassName: org.h2.Driver
      username: sa
      password:
    jpa:
      database-platform: org.hibernate.dialect.H2Dialect
#      hibernate:
#        ddl-auto: none
h2:
  console:
    enabled: true