server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:mem:movies_data
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: none
  h2:
    console:
      enabled: true

additional-info-ms:
  url: localhost:8082